# 工作日志系统规则

## 目的
这个规则帮助AI助手理解和使用工作日志系统，实现工作状态的持久化和无缝对接。

## 文件位置
工作日志文件位于 `docs/workflow-journal/` 目录：
- `current-session.md` - 当前会话状态（实时更新）
- `daily-logs/YYYY-MM-DD.md` - 每日详细日志
- `progress/current-status.md` - 项目进度概览
- `issues/active-issues.md` - 当前问题列表
- `system-updates/changelog.md` - 系统变更记录

## 用户指令

### `@workflow start`
当用户输入 `@workflow start` 时，AI应该：
1. 读取 `docs/workflow-journal/current-session.md`
2. 读取最新的 `docs/workflow-journal/daily-logs/` 文件
3. 读取 `docs/workflow-journal/progress/current-status.md`
4. 读取 `docs/workflow-journal/issues/active-issues.md`
5. 总结上次的工作状态、当前进度、待解决问题
6. 提供今日建议任务列表
7. 更新 `current-session.md` 标记会话开始

### `@workflow end`
当用户输入 `@workflow end` 时，AI应该：
1. 读取 `docs/workflow-journal/current-session.md` 获取今日工作记录
2. 读取Git状态（如果有）了解代码变更
3. 询问用户：
   - 今日完成的主要任务
   - 遇到的问题和解决方案
   - 未完成的工作
   - 明日计划
4. 生成今日日志文件 `daily-logs/YYYY-MM-DD.md`
5. 更新 `progress/current-status.md`
6. 更新 `issues/active-issues.md` 或 `issues/resolved-issues.md`
7. 更新 `current-session.md` 标记会话结束

### `@workflow milestone <描述>`
记录重要里程碑，更新进度文件。

### `@workflow issue <问题描述>`
记录新问题到 `issues/active-issues.md`。

### `@workflow resolve <问题编号> <解决方案>`
标记问题已解决，移动到 `issues/resolved-issues.md`。

## 自动行为

### 代码变更时
- 当创建新文件、修改重要文件时，自动记录到当前会话
- 重要变更（如新增模块、API变更）记录到 `system-updates/changelog.md`

### 遇到错误时
- 自动询问是否记录到问题列表
- 如果用户解决，询问是否记录解决方案

## 日志格式要求
- 使用Markdown格式
- 时间戳格式：YYYY-MM-DD HH:mm
- 使用emoji增强可读性（✅🚧❌🐛💡📝🔄等）
- 保持结构清晰，便于快速扫描

## 读取优先级
1. 先读取 `current-session.md` 了解当前状态
2. 再读取 `current-status.md` 了解整体进度
3. 最后读取最新的 `daily-logs/` 文件获取细节

## 更新原则
- 实时更新 `current-session.md`
- 每日结束时更新 `daily-logs/`
- 进度重大变化时更新 `current-status.md`
- 问题解决时立即更新 `issues/`

